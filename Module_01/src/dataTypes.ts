// Primitive Data Types

/* 
    implicit --> suggested though not directly expressed
    explicit --> stated clearly and in detail, leaving no room for confusion or doubt.

    # When type infer by TypeScript that will implicit data types
    # When type define by programmer that will explicit data types
*/



// string
const institueName: string = "Programming Hero";

// number
const establishedYear: number = 2020;

// boolean
const isFamous: boolean = true;

// undefined
const a: undefined = undefined;

// null
const b: null = null;

// Non Primitive Data Types

// Array

const cousinList: string[] = ["Fahim", "Imram", "Jarif", "Jaifa", "Promi", "Nuha"];

const age: number[] = [10, 8, 4, 1, 16, 9];

// tuple

const ageName: [string, number] = ["Yeasin", 23];

ageName[0] = "Arafat";