// Referenc type - array

// Array with string
const littleBrothers: Array<string> = ["Poyel", "Sayed", "Juwel", "Efat", "Nishad"];
const friends: string[] = ["Asif", "Forkan", "Siam"];

// Array with number
const ageOfCousins: Array<number> = [17, 10, 9, 8, 4, 0.4];
const ageOfFriends: number[] = [23, 20, 22, 34, 60];

// Array with sub array
const subArray: number[][] = [[3], [4], [5]];

// Empty array & push
const empty: number[] = [];

empty.push(4);
empty.push(10);

console.log(empty.length);